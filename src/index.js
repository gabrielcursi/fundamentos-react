import './index.css'
import ReactDOM from 'react-dom' //busca dentro de node_modules o arquivo
import React from 'react' //busca dentro de node_modules o arquivo

import App from './App'

//ReactDOM é o mesmo nome que faz no import
ReactDOM.render(
    <App />,
    document.getElementById('root'))